Copy the following content:

markdown
Copy code
# Git Commands

## Initial Setup
```bash
git init
git config --global user.name "Daya"
git config --global user.email "dayamoger14@gmail.com"
Creating and Managing Repositories
bash
Copy code
mkdir Git-for-devops
cd Git-for-devops/
File Operations
bash
**Copy code**
touch AWS DEvOps
touch nibba.txt nibbi.txt
touch noobi nooba
touch nibbu.txt
touch prod_set_up
**Git Status and Logs**
bash
Copy code
git status
git log
**Adding and Committing Changes**
bash
Copy code
git add nibbi.txt
git add nibba.txt
git add nooba
git commit -m "add nibba nibbi"
git commit -m "add nibba"
git commit -m "commit nooba"
git commit -m "added nibbu"
git commit -m "add prod_set_up"
**Branching**
bash
Copy code
git checkout -b Dev
git checkout -b Prod_set_UP
git checkout master
git checkout Dev
**Removing Files from Staging**
bash
Copy code
git rm --cached nibba.txt
**Viewing History**
bash
Copy code
git log --oneline
**Miscellaneous**
bash
Copy code
ls -ltr
ll
clear
Copy code
